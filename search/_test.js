const { search } = require("./Search");
const { search: revertSearch } = require("./RevertSeach");

/**
 * Search
 */
// const nums = [-1, 0, 3, 5, 9, 12];
// console.log(search(nums, 9));

/**
 * RevertSeach
 */
let nums = [1];
console.log(revertSearch(nums, 1));
